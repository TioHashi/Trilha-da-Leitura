{
  "name": "trilha-da-leitura-functions",
  "version": "1.0.0",
  "private": true,
  "description": "Backend do Assistente Pedagogico com IA da Trilha da Leitura.",
  "type": "module",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "test": "npm run build && node --test tests/*.test.mjs",
    "typecheck": "tsc --project tsconfig.json --noEmit"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.0.0",
    "openai": "^6.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.9.0"
  },
  "engines": {
    "node": "22"
  }
}
